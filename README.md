# Global_Mobility_Application_Analyser

## Workflow

1. constant
2. config_entity
3. artifact_entity
4. component
5. pipeline
6. app.py / demo.py

# How to run
1. Clone the repository and install dependencies:
    ```bash
        git clone https://github.com/aswinvrpai/Global_Mobility_Application_Analyser
        cd Global_Mobility_Application_Analyser
        pip install -r requirements.txt
        python main.py
    ```
2. Create and activate a virtual environment (optional but recommended):
       ```bash
            python -m venv venv
            source venv/bin/activate  # On Windows use `venv\Scripts\activate`
       ```
3. Run the main script:
       ```bash  
            python main.py
         ```
4. Export the mongo db url
        ```bash
            export MONGO_DB_URL="your_mongo_db_url"
        ```

